import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Nuclei Segmentation — Automated U-Net Analysis",
  description:
    "AI-powered automated nuclei segmentation in microscopy images. Published research: KEC 2025 / InJET. 97.5% accuracy, 0.88 IoU.",
  keywords: ["nuclei segmentation", "U-Net", "microscopy", "deep learning", "biomedical AI"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
